//polar coordinates

// have a point xy/ an angle(theta) and a distance(radius). how to find another point? 
// cartesion= x,y in two dimensional space 
//heading gets the current angle=theta 
//mag gets the distances. 
// cos(theta) * m sin(theta) *m= gets the new point. 

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}
